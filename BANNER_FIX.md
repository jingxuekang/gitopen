# 轮播图修复说明

## 问题
轮播图样式文件损坏，导致轮播图不显示。

## 已修复
重新添加了完整的 3D 堆叠轮播图样式：

### 样式特点
1. **容器**：`width: 100%`, `height: 400rpx`
2. **卡片**：`width: calc(100% - 48rpx)`, `height: 360rpx`
3. **非活动卡片**：`scale(0.85)` + `opacity: 0.6`
4. **活动卡片**：`scale(1)` + `opacity: 1`
5. **左右露出**：`previous-margin: 20rpx`, `next-margin: 20rpx`

### 效果
- 中间卡片最大最清晰
- 左右卡片缩小且半透明
- 平滑的缩放过渡动画
- 自动播放 + 循环

## 刷新小程序即可看到轮播图
